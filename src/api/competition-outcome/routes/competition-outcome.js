'use strict';

/**
 * competition-outcome router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::competition-outcome.competition-outcome');
